feat: optimize extraction and JSONB lookup performance

Implement memory-efficient PDF text streaming and advanced GIN indexing to improve system scalability and query performance.

- What: Refactored the text extractor to use generators and added a jsonb_path_ops GIN index to the database.
- How: Introduced stream_text_with_metadata in the Python engine to yield spans iteratively and created a Supabase migration for the manifest indexing.
- Why: To prevent memory exhaustion during large PDF processing and to optimize block-level lookups within complex codex manifests.
