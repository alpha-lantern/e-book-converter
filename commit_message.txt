feat: implement disassociated metadata & hybrid sync

Implement a disassociated metadata architecture to decouple functional book data from crawler-optimized SEO fields. This ensures the Dashboard UI and search engine crawlers can have independent, optimized content.

What: Introduced new metadata columns in the 'books' table and a 'Hybrid-Sync' PostgreSQL trigger to automate JSON manifest updates.
How: Added columns ('author', 'seo_title', 'seo_description', 'seo_tags') and implemented a 'SECURITY DEFINER' trigger function using JSONB merge operators. Updated Python Pydantic models and added comprehensive tests.
Why: To provide a production-ready system for metadata management where the parser's "best guess" can be easily overridden by users in the dashboard without manual JSON manipulation.
