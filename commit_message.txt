feat: implement toggleable sign-up flow & auto-profile DB trigger

Add user registration capability to the admin dashboard UI and database layers using Supabase.

- What: Refactored the login screen to support toggleable Login/Sign-Up modes, implemented a PostgreSQL trigger for automatic profile generation, added widget test coverage, and placed TODO comments for production security hardening.
- How: Added a trigger function on auth.users in a new migration, added `signUp` method to `AuthRepository`, added new validated form fields to `LoginScreen`, updated `widget_test.dart` with toggle behavior validation, and injected security TODO comments in input validators and registration endpoints.
- Why: To enable administrators to self-register locally with secure database consistency (satisfying foreign key constraints on owner_id), verify layout interactions, and track production security requirements.
