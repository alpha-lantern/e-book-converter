feat(engine): define strict CodexBlock data models & optimize database and engine performance

Implement strictly typed Pydantic models to represent content blocks within the Codex parsing engine.

What:
- Defined `CodexBlockType` Enum (h1, h2, p, image, widget).
- Defined `CodexBBox` model for strict coordinate validation.
- Defined `CodexBlock` model with UUID generation and style dicts.
- Added unit tests covering validation, defaults, and typing.
- Updated documentation and session summary.

Why:
- To ensure type safety and data integrity before building the extraction logic (Issue #5).
- `CodexBBox` prevents coordinate indexing errors.

How:
- Created `engine/codex_engine/models.py` and `tests/test_models.py`.

Implements a suite of optimizations across the database and the Python
parsing engine to improve scalability and execution speed.

- Database: Added indexes on foreign keys and functional indexes for
  JSONB metadata to optimize RLS checks and manifest searches.
- Engine: Introduced a specialized 'CodexStyle' Pydantic model to
  replace generic dictionaries, reducing memory overhead and improving
  type safety.
- Engine: Added a computed 'block_map' property to 'CodexManifest' to
  enable O(1) block lookups by UUID, eliminating O(N) traversal costs.
- Docs: Established a technical 'Codex Specification' and updated the
  performance roadmap.
- Tests: Updated the engine test suite to verify new model behaviors
  and O(1) mapping logic.