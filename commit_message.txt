feat(engine): define strict CodexBlock data models

Implement strictly typed Pydantic models to represent content blocks within the Codex parsing engine.

What:
- Defined `CodexBlockType` Enum (h1, h2, p, image, widget).
- Defined `CodexBBox` model for strict coordinate validation.
- Defined `CodexBlock` model with UUID generation and style dicts.
- Added unit tests covering validation, defaults, and typing.
- Updated documentation and session summary.

Why:
- To ensure type safety and data integrity before building the extraction logic (Issue #5).
- `CodexBBox` prevents coordinate indexing errors.

How:
- Added `pydantic` to `pyproject.toml`.
- Created `engine/codex_engine/models.py` and `tests/test_models.py`.
